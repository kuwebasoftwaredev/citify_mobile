import { TestBed } from '@angular/core/testing';

import { socketInterceptor } from './socket.interceptor';

describe('socketInterceptor', () => {
  it('should be created', () => {
    expect(socketInterceptor).toBeTruthy();
  });
});
