import { TestBed } from '@angular/core/testing';

import { timezoneInterceptor } from './timezone-interceptor';

describe('timezoneInterceptor', () => {
  it('should be created', () => {
    expect(timezoneInterceptor).toBeTruthy();
  });
});
